---
import Layout from '../../layouts/Layout.astro';
---

<Layout
  title="Blog — Joyfulness Coöperatie"
  description="Inzichten over leiderschap, bewustzijnsontwikkeling en de overgang naar ecosystemisch organiseren. Door Johan Spaans en Menno Braakman."
  canonicalUrl="https://www.joyfulness.company/blog/"
>

  <!-- HEADER -->
  <header class="site" id="site-header">
    <a href="/" class="logo-link">
      <img src="/images/logo-horizontaal.png" alt="Joyfulness Coöperatie" class="site-logo" />
    </a>
    <button class="nav-toggle" aria-label="Menu openen" aria-expanded="false" aria-controls="site-nav">
      <span></span><span></span><span></span>
    </button>
    <nav class="site-nav" id="site-nav">
      <a href="/blog/" class="active">Blog</a>
      <a href="/quickscan-verticale-leiderschapsontwikkeling/">Quickscan Leiderschap</a>
      <a href="/quickscan-organische-groei/">Quickscan Organische Groei</a>
      <a href="/#contact">Contact</a>
    </nav>
  </header>

  <!-- PAGE HEADER -->
  <div class="blog-page-header">
    <p class="blog-index-kicker">Joyfulness Inzichten</p>
    <h1>Blog</h1>
    <p class="blog-index-sub">Over leiderschap, bewustzijnsontwikkeling en de overgang naar een nieuw paradigma in organiseren.</p>
  </div>

  <!-- ARTICLE LIST -->
  <main class="blog-index-wrap">
    <div class="blog-index-grid">

      <article class="blog-card">
        <a href="/blog/van-machine-naar-ecosysteem/" class="blog-card-img-link" tabindex="-1" aria-hidden="true">
          <img src="/images/blog-paradigma-wortels.jpg" alt="Wortels van bomen verbonden door een lichtgevend netwerk" class="blog-card-img" />
        </a>
        <div class="blog-card-body">
          <p class="blog-card-kicker">Evolutie van Leiderschap</p>
          <h2 class="blog-card-title">
            <a href="/blog/van-machine-naar-ecosysteem/">Van machine naar ecosysteem: waarom het mechanistische paradigma over de datum raakt</a>
          </h2>
          <p class="blog-card-excerpt">Waarom werken verandertrajecten, feedbacktrainingen en KPI-systemen steeds minder? En wat vraagt de overgang naar ecosystemisch organiseren van jou als leider?</p>
          <div class="blog-card-footer">
            <span class="blog-card-meta">Johan Spaans &amp; Menno Braakman &middot; 24 juni 2026</span>
            <a href="/blog/van-machine-naar-ecosysteem/" class="blog-card-read">Lees artikel &rarr;</a>
          </div>
        </div>
      </article>

    </div>
  </main>

  <!-- FOOTER -->
  <footer class="site-footer">
    <p>&copy; 2026 <a href="https://www.joyfulness.company">Joyfulness Coöperatie</a> &mdash; Uw evolutiepartner in leiderschap en organiseren</p>
  </footer>

</Layout>

<style>
  /* HEADER */
  .site {
    display: flex; align-items: center; gap: 0.75rem; flex-wrap: wrap;
    padding: 2.4rem 1.75rem 1.2rem; max-width: var(--max-width); margin: 0 auto;
    position: relative;
  }
  .logo-link { display: flex; align-items: center; flex-shrink: 0; text-decoration: none; }
  .site-logo { height: 80px; width: auto; display: block; }
  @media (max-width: 760px) { .site-logo { height: 60px; } }
  .site-nav { margin-left: auto; display: flex; align-items: center; gap: 1.8rem; }
  .site-nav a {
    font-family: 'Libertad', sans-serif; font-weight: 600; font-size: 0.82rem;
    letter-spacing: 0.02em; color: var(--text); text-decoration: none;
    border-bottom: 1px solid transparent; padding-bottom: 2px; transition: color 0.15s, border-color 0.15s;
  }
  .site-nav a:hover, .site-nav a.active { color: var(--green-dark); border-color: var(--green); }
  .nav-toggle {
    display: none; margin-left: auto; flex-direction: column; justify-content: center;
    gap: 5px; background: none; border: none; cursor: pointer; padding: 4px 0; width: 28px;
  }

  /* PAGE HEADER */
  .blog-page-header {
    background: #fff;
    padding: 4rem 2rem 3.5rem;
    text-align: center;
    border-bottom: 1px solid #e7e1d4;
  }
  .blog-index-kicker {
    font-family: 'LibertadBold', sans-serif;
    font-size: 0.72rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #2F6933;
    margin-bottom: 0.75rem;
  }
  .blog-page-header h1 {
    font-family: 'Golpherd', serif;
    font-size: clamp(2.2rem, 5vw, 3.5rem);
    color: #1c3e20;
    margin-bottom: 0.75rem;
    line-height: 1.1;
  }
  .blog-index-sub {
    font-family: 'Libertad', sans-serif;
    font-size: 1.05rem;
    color: #5c5650;
    max-width: 560px;
    margin: 0 auto;
  }

  /* GRID */
  .blog-index-wrap {
    background: #fff;
    padding: 3rem 2rem 5rem;
  }
  .blog-index-grid {
    max-width: 860px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  /* CARD */
  .blog-card {
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 2px 12px rgba(0,0,0,0.07);
    display: grid;
    grid-template-columns: 320px 1fr;
    transition: box-shadow 0.2s;
  }
  .blog-card:hover { box-shadow: 0 4px 24px rgba(0,0,0,0.12); }
  .blog-card-img-link { display: block; overflow: hidden; }
  .blog-card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.4s ease;
  }
  .blog-card:hover .blog-card-img { transform: scale(1.03); }
  .blog-card-body {
    padding: 1.75rem 2rem;
    display: flex;
    flex-direction: column;
  }
  .blog-card-kicker {
    font-family: 'LibertadBold', sans-serif;
    font-size: 0.7rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #2F6933;
    margin-bottom: 0.5rem;
  }
  .blog-card-title {
    font-family: 'LibertadBold', serif;
    font-size: 1.25rem;
    line-height: 1.35;
    margin-bottom: 0.75rem;
  }
  .blog-card-title a {
    color: #1c3e20;
    text-decoration: none;
    transition: color 0.2s;
  }
  .blog-card-title a:hover { color: #2F6933; }
  .blog-card-excerpt {
    font-family: 'Libertad', serif;
    font-size: 0.92rem;
    color: #5c5650;
    line-height: 1.6;
    flex: 1;
    margin-bottom: 1.25rem;
  }
  .blog-card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .blog-card-meta {
    font-family: 'Noto Sans', sans-serif;
    font-size: 0.75rem;
    color: #9a9088;
  }
  .blog-card-read {
    font-family: 'LibertadBold', sans-serif;
    font-size: 0.82rem;
    color: #2F6933;
    text-decoration: none;
    white-space: nowrap;
    transition: color 0.2s;
  }
  .blog-card-read:hover { color: #1c3e20; }

  /* FOOTER */
  .site-footer {
    background: #fff;
    border-top: 1px solid #e7e1d4;
    color: #9a9088;
    text-align: center;
    padding: 2rem;
    font-family: 'Noto Sans', sans-serif;
    font-size: 0.8rem;
  }
  .site-footer a { color: #5c5650; text-decoration: none; }

  /* MOBILE */
  @media (max-width: 640px) {
    .site { padding: 0.85rem 1rem; }
    .nav-toggle {
      display: flex;
      flex-direction: column;
      gap: 5px;
      background: none;
      border: none;
      cursor: pointer;
      padding: 4px;
    }
    .nav-toggle span {
      display: block; height: 2px; width: 100%; background: var(--text);
      border-radius: 2px; transition: transform 0.22s, opacity 0.22s;
    }
    .nav-toggle { display: flex; }
    .site-nav {
      display: none; width: 100%; flex-direction: column; align-items: flex-start;
      gap: 0; margin-left: 0; padding: 0.4rem 0 0.8rem; border-top: 1px solid var(--border);
    }
    .site-nav.open { display: flex; }
    .blog-page-header { padding: 2.5rem 1.25rem 2rem; }
    .blog-index-wrap { padding: 2rem 1rem 3rem; }
    .blog-card { grid-template-columns: 1fr; }
    .blog-card-img { height: 200px; }
    .blog-card-body { padding: 1.25rem; }
  }
</style>

<script>
  const toggle = document.querySelector('.nav-toggle');
  const nav = document.querySelector('.site-nav');
  toggle?.addEventListener('click', () => {
    const open = nav?.classList.toggle('open');
    toggle.setAttribute('aria-expanded', String(open));
  });
</script>
